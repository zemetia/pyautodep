name: Update PyPI Module
on:
  workflow_dispatch:
  push:
    branches:
      - main
jobs:
  build:
    name: PyPI Update
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v2

      - name: Prepare File
        run: sudo cp README.md /pyautodep
